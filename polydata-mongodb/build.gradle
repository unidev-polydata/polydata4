dependencies {

    api project(":polydata-api")

    implementation(
            "org.mongodb:mongodb-driver-sync:$mongoJavaDriverVersion",
            "com.fasterxml.jackson.core:jackson-core:$jacksonVersion"
    )

    testImplementation(
            "org.redisson:redisson:$testRedissonVersion"
    )
}
